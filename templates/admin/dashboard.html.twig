{% extends '@EasyAdmin/page/content.html.twig' %}

{% block content_title %}Tableau de bord{% endblock %}

{% block main %}
    <div class="container-fluid">
        <div class="row">
            <div class="col-12">
                <div class="alert alert-light border shadow-sm">
                    <h3>Bienvenue sur l'administration de Golden Hour 👋</h3>
                    <p class="mb-0">C'est ici que tu gères tes produits, tes commandes et tes clients.</p>
                </div>
            </div>
        </div>

        <div class="row mt-4">
            {# Raccourci vers les commandes #}
            <div class="col-md-4">
                <div class="card shadow-sm border-0 mb-4">
                    <div class="card-body text-center py-5">
                        <i class="fas fa-shopping-cart fa-3x text-primary mb-3"></i>
                        <h4 class="card-title">Commandes</h4>
                        <p class="card-text text-muted">Voir les achats récents</p>
                        {# EasyAdmin gère les liens dynamiquement, mais on peut laisser vide pour l'instant #}
                    </div>
                </div>
            </div>

            {# Raccourci vers les produits #}
            <div class="col-md-4">
                <div class="card shadow-sm border-0 mb-4">
                    <div class="card-body text-center py-5">
                        <i class="fas fa-box-open fa-3x text-success mb-3"></i>
                        <h4 class="card-title">Catalogue</h4>
                        <p class="card-text text-muted">Gérer les stocks</p>
                    </div>
                </div>
            </div>
            
            {# Raccourci vers le site #}
            <div class="col-md-4">
                <div class="card shadow-sm border-0 mb-4">
                    <div class="card-body text-center py-5">
                        <i class="fas fa-desktop fa-3x text-secondary mb-3"></i>
                        <h4 class="card-title">Boutique</h4>
                        <p class="card-text text-muted">Retourner sur le site</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}